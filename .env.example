# Optional cross-device sync via your own Supabase project (no login).
# Copy to .env and fill in. Leave blank to disable sync (app still works locally).
# Find these in Supabase → Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
